/* eslint-disable @typescript-eslint/no-unused-vars */
import { makeStyles } from "tss-react/mui";

export const useStyles = makeStyles()((theme) => {
  return {
    tab: { fontWeight: 700, color: "#9c27b0" },
  };
});
